# requirements.txt: Dependencies for the xAI Grok Chat App
# Install with: pip install -r requirements.txt

streamlit>=1.20.0  # For the web UI and streaming
openai>=1.0.0  # For API compatibility with xAI
passlib>=1.7.4  # For secure password hashing
python-dotenv>=0.21.0  # For loading .env files
ntplib>=0.4.0  # For NTP time synchronization in the time tool
